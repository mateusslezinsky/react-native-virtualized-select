import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  flatListContainer: {},
  flatListTouchable: {
    marginTop: 10,
    width: "100%",
  },
  flatListTouchableText: {
    fontSize: 16,
  }
})
